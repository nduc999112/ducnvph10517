<head>
   <style>
       tr{
           background-color: #f1f7fc;
       }
   </style>

</head>
<h3>{{titlelogin}}</h3>
<a href="signup" class="btn btn-primary btn-block" type="submit" style="border-radius: 6px;">Add User</a>
<table class="table table-bordered">
    <tr>
        <th>FULL NAME</th>
        <th>USERNAME</th>
        <th>PASSWORD</th>
        <th>EMAIL</th>
        <th>NUMBER PHONE</th>
        <th>ACTION</th>
    </tr>
    <tr>
        <td>1</td>
        <td>1</td>
        <td>1</td>
        <td>1</td>
        <td>1</td>

        <td>
            <a href="#" class="btn btn-primary btn-sm">EDIT</a>
            <a href="#" class="btn btn-danger btn-sm">EDIT</a>
        </td>
    </tr>
    {{#each ups}}
        <tr>
            <td>{{this.name}}</td>
            <td>{{this.username}}</td>
            <td>{{this.passwor}}</td>
            <td>{{this.email}}</td>
            <td>{{number_phone}}</td>

            <td>
                <a href="/{{this._id}}" class="btn btn-primary btn-sm">EDIT</a>
                <a href="/delete/{{this._id}}" onclick="return confirm('Are you sure ?')" class="btn btn-danger btn-sm">DELETE</a>
            </td>
        </tr>
    {{/each}}
</table>
